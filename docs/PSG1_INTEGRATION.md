# PSG1 Integration Notes (extracted)

This document contains the PSG1 integration guidance copied from the original RPS v2 repo. It includes mapping notes, TWA packaging details, and developer guidance for mounting the PSG1 simulator.

- Device: PlaySolana Gaming 1 (PSG1)
- Integration points: `apps/web/src/hooks/useGamepad.ts`, `apps/web/src/lib/gamepad-nav.ts`, `apps/web/src/components/GamepadDebugBridge.tsx`, `apps/web/src/components/VirtualKeyboard.tsx`.

See the `PSG1/` directory for build helpers and PlayGate submission docs.
