# PSG1 Integration — RPS v2 (copied)

This folder contains the PSG1-specific packaging and submission helpers.

Files:
- `CONTROLLER_MAP.md` — button mapping and notes
- `PLAYGATE.md` — PlayGate submission checklist and copy
- `twa-manifest.json` — TWA manifest tuned for PSG1
- `build-psg1.ps1` — PowerShell helper to build the TWA APK via Bubblewrap

Usage:
- Run `.uild-psg1.ps1` from this folder to build an APK (requires Java/Android SDK and Bubblewrap)
