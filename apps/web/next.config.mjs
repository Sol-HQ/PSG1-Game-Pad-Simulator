/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow img tags from the same origin (simulator uses /art/ and /brand/)
  // No remote images needed — everything is local
};

export default nextConfig;
